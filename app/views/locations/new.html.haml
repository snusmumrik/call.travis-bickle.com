.container
  .row
    .col-lg-10.col-lg-offset-1
      %h2.text-center= t("activerecord.models.location") + t("views.new")
      = render 'form'
      = link_to t("links.back"), locations_path, class: "btn btn-default"

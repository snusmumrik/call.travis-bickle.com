.container
  .row
    .col-lg-10.col-lg-offset-1
      .text-center
        %h2= t("activerecord.models.order") + t("views.edit")

      = render 'form'

      = link_to 'Show', @order
      \|
      = link_to 'Back', orders_path

.container
  .row
    .col-lg-10.col-lg-offset-1
      %h2.text-center= t("activerecord.models.order") + t("views.new")
      = render 'form'
      = link_to t("links.back"), orders_path, class: "btn btn-default"
